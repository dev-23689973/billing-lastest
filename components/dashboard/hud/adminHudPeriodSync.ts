/** Session + window event used by `AdminAppHeader` and `DashboardPeriodProvider` so one strip drives charts. */
export const ADMIN_HUD_PERIOD_KEY = "admin-hud-period";
export const ADMIN_HUD_PERIOD_EVENT = "admin-hud-period";
