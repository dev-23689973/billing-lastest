"use client";

/**
 * Previously injected a reduced `cols` query on mobile. Default is now “all columns”
 * when `cols` is omitted (`parseStaffListColsFromSearchParam`), so this is a no-op.
 */
export function StaffListMobileColsBootstrap() {
  return null;
}
